document.addEventListener("DOMContentLoaded", function () {
    AOS.init({
      duration: 1000,  // Duración de la animación
      easing: "ease-in-out",
      once: true  // La animación ocurre una vez
    });
  });
  